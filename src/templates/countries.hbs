{{#each this}}
<li>
    <img src={{flag}} width="100">
    <h4>{{name}}</h4>
    <h5>{{capital}}</h5>
    <ul>
        {{#each languages}}
        <li>{{this.name}}</li>
        {{/each}}
    </ul>
    <p>{{population}}</p>

</li>
{{/each}}